import { HideOnErrorDirective } from './hide-on-error.directive';

describe('HideOnErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new HideOnErrorDirective();
    expect(directive).toBeTruthy();
  });
});
